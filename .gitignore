build/
build_*/
*.hex
*.bin
.DS_Store
__pycache__/
.venv/
# Sesje pomiarowe trybu autonomicznego (dane, mogą być duże) i lokalne
# plany. Wzór planu (plans/*.example.toml) trafia do repo.
reports/sessions/
plans/*.toml
!plans/*.example.toml
# Prywatny manifest scenariuszy (własne/cudze wpisy `source`) – scala się
# na wierzch scenarios.toml, ale zostaje lokalny.
scenarios.local.toml
# Dziennik pomiarów – dane lokalne, nie trzymamy ich na remote.
reports/pomiary.csv
